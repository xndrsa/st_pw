services:
  - type: web
    name: sat-navigator-api
    env: python
    pythonVersion: 3.11.9
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps chromium
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    plan: free

